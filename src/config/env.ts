// Central place for runtime constants.
// If you don't want to set environment variables manually, fill the placeholders below.
// NOTE: Do NOT commit real secrets to public repos.

export const OSU_CLIENT_ID: string | undefined = process.env.OSU_CLIENT_ID;
export const OSU_CLIENT_SECRET: string | undefined = process.env.OSU_CLIENT_SECRET;
